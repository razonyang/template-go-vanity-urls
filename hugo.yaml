baseURL: https://pkg.razonyang.com

disableKinds:
  - sitemap
  - taxonomy
  - term

outputs:
  home: ["HTML"]

module:
  imports:
    - path: github.com/hugomods/go-vanity-urls

params:
  go_vanity_urls:
    # host: pkg.razonyang.com # optonal, default is baseURL.
    vcs: git
